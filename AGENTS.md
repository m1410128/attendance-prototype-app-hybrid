# AGENTS.md

このファイルを最初に確認し、この内容を前提として作業してください。

## 現在進行中のプロジェクトについて
HTML/CSSに、データ保存先をGoogle Spreadsheet(GAS)を経由する形で社用車の使用状況管理アプリを開発しています。

## 構造
- 当初はHTML/CSSで開発を進め、GitHubでリンクを共有する形で配布しようとしていたが、データの置き場がlocalStorageだとブラウザ依存になり入力内容が共有されないことが判明した。
- よって、現在使用している社用アカウントのGoogle Spreadsheet経由でデータをやり取りすることで、社員Aの入力結果を社員Bが見られるようなデータ構造を想定している。

- この当初進めていたパッケージを「ローカル版(Desktop\Python\web開発練習\勤務管理アプリ)」・ローカル版の機能全てを完全移植しようとして頓挫したものを「GAS版(Desktop\Python\web開発練習\勤務管理アプリ_GAS版」・それの機能を移植しデータ管理を拡張させ実用に耐えうる版を「ハイブリッド版(Desktop\Python\web開発練習\勤務管理アプリ_ハイブリッド版)」とする。

- このハイブリッド版には、ローカル版のフロントエンドを可能な限り維持し、GASは最低限Google Spreadsheetとのデータのやり取りを担当するWeb APIとして利用する想定です。

## 作業ルール
- トークンの消費を抑え月制限に引っかかりにくくするため、以下のルールを順守してもらう

- 1回のタスクにつき1機能の実装に抑える
- 複数の機能を同時に実装しようとしない
- 必要なファイルだけ確認する
- 同じファイルを繰り返し読み込まない
- 不要なファイルは読み込まない
- 推測で関連ファイルを開かない
- Userからの指示がない限り、勝手にコーディングを開始しない
- 指示された範囲外のコードは変更しない

- 作業を始める前に、今回実施する内容を1~3行で簡潔に述べる
- Userからの指示内容が曖昧であればコーディングせず質問をする

- 新規作成・変更した関数の先頭にその役割・機能の説明を1行程度コメントアウトで記載する

## 基本方針
- ハイブリッド版に既存の構造、つまりローカル版の機能をなるべく再現する。ローカル版の移植で十分な場合、codex側ではなくuser側が手動で行う。
- 変更は必要最小限に留める 大規模な設計変更が予測されるならば、一旦提案に留め、Userの了承を得てから実施する
- 不明な点は推測せず確認を取る

## 編集対象
- ハイブリッド版の内部のファイルに限定する
- ただし、現在AGENTES.md執筆中の段階ではハイブリッド版のフォルダに何もないので、将来的な想定である
- 都度、Userが編集を許可したファイルも対象とする。その際、読み込む前に確認を取ること

## 編集しないもの
- 上記以外の全て

## 読み込むファイル
- 基本的に、Userが指定したファイルのみ読み込む
- ローカル版(勤務管理アプリ)およびGAS版(勤務管理アプリ_GAS版)内のファイルは、ハイブリッド版へ機能を移植する際の参考資料として扱う
- 必要になった場合は追加で確認するファイルを提案し、了承を得てから読む

## 保存先となるスプレッドシート
- Spreadsheet ID:1FRQZtMRTu7PEPb2c9kouBel9_uGUv6jEM6jQS6qSgpo
- シート名:社用車 予約管理表 データ保存場所(仮)

-主な列
A: 予約ID
B: 利用者
C: 車両
D: 開始日時
E: 終了日時
F: (用途不明 思い出したら追記)
G: 開始時刻
H: 終了時刻
I: 状態(ステータス)

- Spreadsheet ID:1Md6vLXkL_asztUESwtodvEdZHHNl1U_NXgzpPC7Yef8
- シート名:社用車 予約管理表 記入ログ(仮)

- 主な列
A: 予約ID
B: 利用者
C: 操作日時
D: 走査種別

列の役割は適宜追加・編集・削除しても可

## 保存形式

- 入力画面では開始日～終了日を展開し、日付ごとに1レコード生成する。
- 生成したレコードは配列(JSON)として保持する。
- Google Apps Scriptには1回のPOSTで配列全体を送信する。
- GAS側では配列をsetValues()で一括書き込みする。
- 1件ずつgoogle.script.runやfetchを繰り返す実装は禁止。

## 移行方針
- localStorageからGoogle Spreadsheetへ段階的に移行する。
- 各ステップで動作確認を行い、一度に入力・保存・表示をすべて変更しない。
- 保存処理→送信処理→表示処理の順で実装・検証する。

## 回答方針

- 原因を推測だけで断定しないでください。

- エラーの原因を報告する場合は、「実際のソースコードのどの行が原因なのか」「なぜそのコードでそのエラーになるのか」をセットで説明してください。

- 文字化けしたコードや推測だけで修正案を提示しないでください。
- 変更理由を簡潔に説明する
- 関連しないリファクタリングは行わない

## 解析時のルール

- このプロジェクトのソースコードはすべてUTF-8として扱ってください。
- 日本語や記号が文字化けしている場合は、ソースコードの破損ではなく文字コードの読み取りミスを疑ってください。
- 文字化けした状態を根拠にHTMLやJavaScriptの構文エラーと判断しないでください。
- 構文エラーを指摘する場合は、必ず実際のソースコードの該当箇所を引用して説明してください。

## テスト方針
- テスト・実行・デバッグはUserの指示がある場合にのみ行う